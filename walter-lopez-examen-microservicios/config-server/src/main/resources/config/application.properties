# ==========================================================================
#  Config COMPARTIDA por TODOS los servicios (perfil por defecto = local)
#  Lo que este aqui lo heredan personas, solicitudes y notificaciones.
# ==========================================================================

# La URL de Eureka: antes estaba repetida en cada servicio. Ahora, UNA sola vez.
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Ejemplo de propiedad de negocio centralizada (leela con @Value("${saludo.mensaje}"))
saludo.mensaje=Hola, configuracion centralizada desde el Config Server
